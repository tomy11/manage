export interface User {
    id: string;
    name: string;
    email: string;
    active: boolean;
}

export type IUser = {
    id: string,
    firstName: string,
    lastName: string,
    email: string,
    password: string,
}